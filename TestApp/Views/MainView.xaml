<Window x:Class="TestApp.Views.MainView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="MainView" 
		Height="400" 
		Width="700">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<StackPanel>
			<Button x:Name="OpenSecondWindow" Content="Second Window" Margin="10" />
			<Button x:Name="OpenChildWindow" Content="Child Window" Margin="10" />
		</StackPanel>
	</Grid>
</Window>
